import 'package:flutter/material.dart';

class BlankIndex extends StatelessWidget {
  const BlankIndex({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}