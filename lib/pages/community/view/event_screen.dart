import 'package:flutter/material.dart';

class EvenetScreen extends StatelessWidget {
  const EvenetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          
        ],
      ),
    );
  }
}